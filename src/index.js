import React from 'react';
import ReactDOM from 'react-dom';
import GameApp from './GameApp';
import './index.css';

const divRoot = document.querySelector('#root');

ReactDOM.render( <GameApp />, divRoot);